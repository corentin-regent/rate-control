version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "latest"
  commands:
    - pip install poetry
    - poetry install --with docs
    - poetry run sphinx-build ./docs $READTHEDOCS_OUTPUT/html
